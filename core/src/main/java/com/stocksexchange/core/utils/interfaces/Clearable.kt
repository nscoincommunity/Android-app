package com.stocksexchange.core.utils.interfaces

/**
 * An interface to implement if the view data is clearable
 * (that is, its data is able to be cleared).
 */
interface Clearable {

    /**
     * Clears the data.
     */
    fun clear()

}